export interface Project {
  title: string;
  image: string;
  imageDimensions: { width: number; height: number };
  summary: string;
  detail: string;
  skills: string;
  link: string;
  linkLabel: string;
}
